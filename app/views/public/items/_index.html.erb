<div class="row">
  <% items.each do |item| %>
      <div class="col-md-3">
        
        <%= link_to item_path(item.id) do %>
        <%= attachment_image_tag item, :image, :fill, 150, 100, format: 'jpeg',fallback: "no-image-icon.jpg",size: '150x100'%><br>
        <%= item.name %><br>
        <% end %>
        ¥<%= item.add_tax_price.to_s(:delimited) %>
      </div>
        <% end %>
  </div>
</div>
