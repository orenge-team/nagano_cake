<div class='row'>
  <% items.each do |item| %>
    <div class="col-12, m-3">
       <%= link_to item_path(item.id) do %>
       <%= attachment_image_tag item, :image, :fill, 200, 150, format: 'jpeg',fallback: "no-image-icon.jpg",size: '200x150'%><br>
       <%= item.name %><br>
       <% end %>
       ¥<%= item.add_tax_price.to_s(:delimited) %><br>
   
    </div>
    <% end %>
</div>

