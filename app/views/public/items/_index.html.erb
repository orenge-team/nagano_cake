<% items.each do |item| %>
  <%= link_to item_path(item.id) do %>
    <%= attachment_image_tag item, :image, :fill, 150, 100, format: 'jpeg',fallback: "no-image-icon.jpg",size: '150x100'%>
    <%= item.name %>
  <% end %>
  ¥<%= item.add_tax_price.to_s(:delimited) %>
<% end %>